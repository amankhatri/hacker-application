<%= form_for(@award) do |f| %>
  <% if @award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@award.errors.count, "error") %> prohibited this award from being saved:</h2>

      <ul>
        <% @award.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :award_winners %><br />
    <%= f.text_field :award_winners %>
  </div>
  <div class="field">
    <%= f.label :award_name %><br />
    <%= f.text_field :award_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :recieved_on %><br />
    <%= f.date_select :recieved_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
